name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: 
     - ubuntu-latest
     - windows-latest
    
    steps:
    - uses: actions/checkout@v2
    - run: make update
    - run: make patch
    - run: make all
